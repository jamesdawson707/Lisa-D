<!DOCTYPE html>
<html>
  <head>
    <title>Lisa D - Contact</title>
  </head>
  <body>
    <div class="container">
      <div class="row">
        <h1 class="text-center">Contact</h1>
        
        <h3>713 Military Road, Mosman</h3>
        <p>Corner of Gouldsbury St and Military Rd</p>
        <h3>Open 10 - 5, 7 days</h3>
        <h3>02 9969 6958</h3>
        <br>
        <p>Or leave us a note and we will email you back!</p>
    
        <div class="col-md-6 col-md-offset-3">
          <% flash.each do |type, msg| %>
            <%= content_tag :div, msg, class: "alert alert-#{type}" %>
          <% end %>      
          <div class="well">
            <%= form_for @contact do |f| %>
              <div class="form-group">
                <%= f.label :name %>
                <%= f.text_field :name, class: 'form-control' %>
              </div>
              <div class="form-group">
                <%= f.label :email %>
                <%= f.text_field :email, class: 'form-control' %>
              </div>
              <div class="form-group">
                <%= f.label :comments %>
                <%= f.text_area :comments, class: 'form-control' %>
              </div>
              <%= f.submit 'Submit', class: 'btn btn-default' %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>